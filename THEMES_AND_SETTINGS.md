# ZX Spectrum Emulator - Настройки и темы

## 🎨 Поддержка тем

Приложение поддерживает автоматическое определение и применение тем:

### Доступные темы:
- **Системная** (по умолчанию) - следует настройкам операционной системы
- **Светлая** - принудительно светлая тема
- **Темная** - принудительно темная тема

### Переключение темы:
Меню → Настройки → Тема → выберите нужную тему

## 💾 Сохранение настроек

Настройки автоматически сохраняются в YAML файл:
- **Расположение**: `~/.spectrum-emulator/settings.yml`
- **Автосохранение**: при каждом изменении настроек
- **Автозагрузка**: при запуске приложения

### Сохраняемые настройки:
- Выбранная тема
- Размер и положение окна
- Последние пути к ROM и снэпшотам
- Настройки эмулятора (масштаб, быстрая загрузка)

## 🔧 Технические детали

### Определение системной темы:
- **macOS**: `defaults read -g AppleInterfaceStyle`
- **Windows**: реестр `HKCU\Software\Microsoft\Windows\CurrentVersion\Themes\Personalize\AppsUseLightTheme`
- **Linux**: переменная `GTK_THEME` и `gsettings`

### Диалоговые окна:
Все диалоги (О программе, Настройки) автоматически наследуют текущую тему приложения.

### CSS файлы тем:
- `src/main/resources/css/light-theme.css` - светлая тема
- `src/main/resources/css/dark-theme.css` - темная тема

## 📁 Структура настроек

Пример файла `~/.spectrum-emulator/settings.yml`:

```yaml
theme: "SYSTEM"
zoomLevel: 2
fastLoad: false
windowWidth: 744.0
windowHeight: 716.0
windowMaximized: false
lastRomPath: ""
lastSnapshotPath: ""
```